# Efficient-Channel-Aware-Convolutional-Neural-Network
The recently proposed Efficient Channel-Aware Convolutional Neural Network (ECAS-CNN) enhances classification accuracy using an adaptive channel attention mechanism (ECA module), achieving strong results on the GTZAN dataset.
